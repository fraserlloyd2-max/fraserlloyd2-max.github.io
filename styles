/* ========================================
   CSS RESET & BASE STYLES
======================================== */

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    /* Primary Colors - F1 Inspired with Navy Backgrounds */
    --primary-red: #E10600;
    --primary-black: #15151E;
    --primary-white: #FFFFFF;
    --navy-blue: #001F3F;
    --navy-light: #003366;
    --navy-dark: #001528;
    
    /* Accent Colors */
    --accent-gold: #FFD700;
    --accent-silver: #C0C0C0;
    --accent-blue: #0090D0;
    
    /* Neutral Colors */
    --gray-50: #F9FAFB;
    --gray-100: #F3F4F6;
    --gray-200: #E5E7EB;
    --gray-300: #D1D5DB;
    --gray-400: #9CA3AF;
    --gray-500: #6B7280;
    --gray-600: #4B5563;
    --gray-700: #374151;
    --gray-800: #1F2937;
    --gray-900: #111827;
    
    /* Typography */
    --font-heading: 'Montserrat', sans-serif;
    --font-body: 'Inter', sans-serif;
    
    /* Spacing */
    --container-width: 1280px;
    --container-narrow: 780px;
    --spacing-xs: 0.5rem;
    --spacing-sm: 1rem;
    --spacing-md: 1.5rem;
    --spacing-lg: 2rem;
    --spacing-xl: 3rem;
    --spacing-2xl: 4rem;
    
    /* Border Radius */
    --radius-sm: 0.375rem;
    --radius-md: 0.5rem;
    --radius-lg: 0.75rem;
    --radius-xl: 1rem;
    
    /* Shadows */
    --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
    
    /* Transitions */
    --transition-fast: 150ms ease;
    --transition-base: 200ms ease;
    --transition-slow: 300ms ease;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: var(--font-body);
    font-size: 16px;
    line-height: 1.6;
    color: var(--gray-200);
    background-color: var(--navy-blue);
    overflow-x: hidden;
}

img {
    max-width: 100%;
    height: auto;
    display: block;
}

a {
    text-decoration: none;
    color: inherit;
    transition: var(--transition-base);
}

ul {
    list-style: none;
}

button {
    font-family: inherit;
    cursor: pointer;
    border: none;
    background: none;
}

/* ========================================
   UTILITY CLASSES
======================================== */

.container {
    max-width: var(--container-width);
    margin: 0 auto;
    padding: 0 var(--spacing-md);
}

.container.narrow {
    max-width: var(--container-narrow);
}

.highlight {
    color: var(--primary-red);
}

/* ========================================
   BUTTONS
======================================== */

.btn {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    font-weight: 600;
    font-size: 0.95rem;
    border-radius: var(--radius-md);
    transition: all var(--transition-base);
    cursor: pointer;
    text-align: center;
}

.btn-primary {
    background: var(--primary-red);
    color: var(--primary-white);
    box-shadow: 0 4px 6px rgba(225, 6, 0, 0.3);
}

.btn-primary:hover {
    background: #C10500;
    transform: translateY(-2px);
    box-shadow: 0 8px 12px rgba(225, 6, 0, 0.5);
}

.btn-secondary {
    background: transparent;
    color: var(--primary-white);
    border: 2px solid var(--primary-red);
}

.btn-secondary:hover {
    background: var(--primary-red);
    transform: translateY(-2px);
    box-shadow: 0 6px 10px rgba(225, 6, 0, 0.4);
}

.btn-outline {
    background: transparent;
    color: var(--primary-black);
    border: 2px solid var(--gray-300);
}

.btn-outline:hover {
    background: var(--primary-black);
    color: var(--primary-white);
    border-color: var(--primary-black);
}

/* ========================================
   NAVIGATION
======================================== */

.navbar {
    background: var(--navy-dark);
    box-shadow: var(--shadow-md);
    position: sticky;
    top: 0;
    z-index: 1000;
    border-bottom: 4px solid var(--primary-red);
}

.nav-wrapper {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 0;
}

.logo h1 {
    font-family: var(--font-heading);
    font-size: 1.75rem;
    font-weight: 800;
    letter-spacing: -1px;
    color: var(--primary-white);
}

.logo .highlight {
    color: var(--primary-red);
}

.nav-menu {
    display: flex;
    gap: 2rem;
    align-items: center;
}

.nav-menu a {
    font-weight: 500;
    font-size: 0.95rem;
    color: var(--gray-300);
    position: relative;
    padding: 0.5rem 0;
}

.nav-menu a::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 3px;
    background: var(--primary-red);
    transition: width var(--transition-base);
}

.nav-menu a:hover::after,
.nav-menu a.active::after {
    width: 100%;
}

.nav-menu a.active {
    color: var(--primary-red);
}

.mobile-toggle {
    display: none;
    flex-direction: column;
    gap: 0.4rem;
}

.mobile-toggle span {
    width: 25px;
    height: 3px;
    background: var(--primary-red);
    border-radius: 3px;
    transition: var(--transition-base);
}

/* ========================================
   HERO SECTION
======================================== */

.hero {
    position: relative;
    background: linear-gradient(135deg, var(--navy-dark) 0%, #1a0a0f 30%, var(--navy-blue) 70%, var(--navy-light) 100%);
    padding: 6rem 0;
    overflow: hidden;
    border-bottom: 5px solid var(--primary-red);
    box-shadow: 0 5px 15px rgba(225, 6, 0, 0.3);
}

.hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('https://images.unsplash.com/photo-1568605117036-5fe5e7bab0b7?w=1600&auto=format&fit=crop&q=80') center/cover;
    opacity: 0.15;
    z-index: 0;
}

.hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(to right, rgba(0, 21, 40, 0.95), rgba(0, 43, 92, 0.7));
    z-index: 1;
}

.hero-content {
    position: relative;
    z-index: 2;
    max-width: 700px;
}

.hero-tag {
    display: inline-block;
    background: var(--primary-red);
    color: var(--primary-white);
    padding: 0.5rem 1rem;
    border-radius: var(--radius-sm);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 1px;
    margin-bottom: 1.5rem;
    box-shadow: 0 4px 6px rgba(225, 6, 0, 0.4);
}

.hero-title {
    font-family: var(--font-heading);
    font-size: 3.5rem;
    font-weight: 800;
    line-height: 1.1;
    color: var(--primary-white);
    margin-bottom: 1.5rem;
    text-shadow: 2px 2px 8px rgba(225, 6, 0, 0.4);
}

.hero-description {
    font-size: 1.15rem;
    color: var(--gray-300);
    margin-bottom: 2rem;
    line-height: 1.7;
}

.hero-buttons {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
}

/* ========================================
   MAIN CONTENT
======================================== */

.main-content {
    padding: var(--spacing-2xl) 0;
    background: linear-gradient(180deg, var(--navy-blue) 0%, #0a0a14 50%, var(--navy-blue) 100%);
}

.content-grid {
    display: grid;
    grid-template-columns: 1fr 350px;
    gap: 3rem;
}

/* ========================================
   BLOG POSTS
======================================== */

.section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
    flex-wrap: wrap;
    gap: 1rem;
}

.section-header h2 {
    font-family: var(--font-heading);
    font-size: 2rem;
    font-weight: 700;
}

.filter-tags {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
}

.tag-btn {
    padding: 0.5rem 1rem;
    font-size: 0.875rem;
    font-weight: 500;
    border-radius: var(--radius-md);
    background: var(--gray-100);
    color: var(--gray-700);
    transition: var(--transition-base);
}

.tag-btn:hover,
.tag-btn.active {
    background: var(--accent-blue);
    color: var(--primary-white);
}

/* Post Cards */
.post-card {
    background: var(--primary-white);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-md);
    transition: all var(--transition-base);
    height: 100%;
    display: flex;
    flex-direction: column;
}

.post-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-xl);
}

.post-card.featured {
    margin-bottom: 3rem;
    max-width: 100%;
    max-height: 600px;
}

.post-image {
    position: relative;
    overflow: hidden;
    aspect-ratio: 16 / 10;
}

.post-card.featured .post-image {
    aspect-ratio: 16 / 8;
    max-height: 350px;
}

.post-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-slow);
}

.post-card:hover .post-image img {
    transform: scale(1.05);
}

.post-badge {
    position: absolute;
    top: 1rem;
    left: 1rem;
    background: var(--accent-blue);
    color: var(--primary-white);
    padding: 0.5rem 1rem;
    border-radius: var(--radius-sm);
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.post-content {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    flex: 1;
}

.post-meta {
    display: flex;
    gap: 1rem;
    align-items: center;
    margin-bottom: 1rem;
    font-size: 0.875rem;
}

.post-meta .category {
    color: var(--accent-blue);
    font-weight: 600;
}

.post-meta .date {
    color: var(--gray-500);
}

.post-title {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    font-weight: 700;
    line-height: 1.3;
    margin-bottom: 0.75rem;
}

.post-card.featured .post-title {
    font-size: 1.75rem;
    margin-bottom: 1rem;
}

.post-title a {
    color: var(--gray-900);
}

.post-title a:hover {
    color: var(--accent-blue);
}

.post-excerpt {
    color: var(--gray-600);
    line-height: 1.6;
    margin-bottom: 1rem;
    flex: 1;
}

.post-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 1rem;
    border-top: 1px solid var(--gray-200);
    font-size: 0.875rem;
}

.author {
    display: flex;
    align-items: center;
    gap: 0.75rem;
}

.author img {
    width: 32px;
    height: 32px;
    border-radius: 50%;
}

.author strong {
    color: var(--gray-900);
}

.read-time {
    color: var(--gray-500);
}

/* Posts Grid */
.posts-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    margin-bottom: 3rem;
}

.load-more {
    text-align: center;
}

/* ========================================
   SIDEBAR
======================================== */

.sidebar {
    display: flex;
    flex-direction: column;
    gap: 2rem;
}

.widget {
    background: var(--primary-white);
    padding: 1.5rem;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
}

.widget h3 {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 1rem;
}

/* Subscribe Widget */
.subscribe-widget p {
    color: var(--gray-600);
    margin-bottom: 1rem;
    font-size: 0.9rem;
}

.subscribe-form {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
}

.subscribe-form input {
    padding: 0.75rem;
    border: 1px solid var(--gray-300);
    border-radius: var(--radius-md);
    font-family: inherit;
    font-size: 0.9rem;
}

.subscribe-form input:focus {
    outline: none;
    border-color: var(--accent-blue);
}

/* Standings Widget */
.standings-list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
}

.standings-list li {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.75rem;
    background: var(--gray-50);
    border-radius: var(--radius-md);
    font-size: 0.9rem;
}

.standings-list .position {
    font-weight: 700;
    font-size: 1.1rem;
    color: var(--primary-red);
    min-width: 1.5rem;
}

.standings-list .driver {
    flex: 1;
    font-weight: 600;
    color: #000000;
}

.standings-list .points {
    font-weight: 700;
    color: #000000;
}

.view-all {
    display: inline-block;
    margin-top: 1rem;
    color: var(--accent-blue);
    font-weight: 600;
    font-size: 0.9rem;
}

.view-all:hover {
    text-decoration: underline;
}

/* Popular Posts Widget */
.popular-posts {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.popular-post {
    display: flex;
    gap: 1rem;
}

.popular-post img {
    width: 80px;
    height: 80px;
    object-fit: cover;
    border-radius: var(--radius-md);
}

.popular-content h4 {
    font-size: 0.9rem;
    font-weight: 600;
    line-height: 1.4;
    margin-bottom: 0.25rem;
}

.popular-content a:hover {
    color: var(--accent-blue);
}

.popular-content .post-date {
    font-size: 0.8rem;
    color: var(--gray-500);
}

/* Categories Widget */
.categories-list {
    display: flex;
    flex-direction: column;
}

.categories-list li {
    border-bottom: 1px solid var(--gray-200);
}

.categories-list li:last-child {
    border-bottom: none;
}

.categories-list a {
    display: flex;
    justify-content: space-between;
    padding: 0.75rem 0;
    color: var(--gray-700);
    font-size: 0.95rem;
}

.categories-list a:hover {
    color: var(--accent-blue);
}

.categories-list span {
    color: var(--gray-500);
}

/* ========================================
   ARTICLE PAGE
======================================== */

.article-page {
    background: var(--primary-white);
}

.article-header {
    padding: 3rem 0 2rem;
    background: var(--gray-50);
}

.breadcrumbs {
    font-size: 0.875rem;
    color: var(--gray-600);
    margin-bottom: 1.5rem;
}

.breadcrumbs a:hover {
    color: var(--primary-red);
}

.article-meta-top {
    display: flex;
    gap: 1rem;
    margin-bottom: 1rem;
    font-size: 0.875rem;
}

.article-meta-top .category {
    color: var(--primary-red);
    font-weight: 600;
}

.article-meta-top .date {
    color: var(--gray-600);
}

.article-title {
    font-family: var(--font-heading);
    font-size: 3rem;
    font-weight: 800;
    line-height: 1.2;
    margin-bottom: 2rem;
    color: #000000;
}

.article-author {
    display: flex;
    align-items: center;
    gap: 1rem;
}

.article-author img {
    width: 56px;
    height: 56px;
    border-radius: 50%;
}

.author-info {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
}

.author-info strong {
    font-size: 1rem;
    color: #000000;
}

.author-info span {
    font-size: 0.875rem;
    color: var(--gray-600);
}

.article-featured-image {
    max-width: 1400px;
    margin: 0 auto;
    padding: 2rem 1rem;
}

.article-featured-image img {
    width: 100%;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-xl);
}

.article-content {
    padding: 3rem 0;
}

.article-content .lead {
    font-size: 1.25rem;
    line-height: 1.7;
    color: #000000;
    font-weight: 500;
    margin-bottom: 2rem;
    background: var(--primary-white);
    padding: 1.5rem;
    border-radius: var(--radius-md);
}

.article-content p {
    margin-bottom: 1.5rem;
    font-size: 1.05rem;
    line-height: 1.8;
    color: #000000;
    background: var(--primary-white);
    padding: 1rem 1.5rem;
    border-radius: var(--radius-md);
}

.article-content h2 {
    font-family: var(--font-heading);
    font-size: 2rem;
    font-weight: 700;
    margin: 3rem 0 1.5rem;
    color: #000000;
    background: var(--primary-white);
    padding: 1rem 1.5rem;
    padding-left: 2rem;
    border-left: 5px solid var(--primary-red);
    border-radius: var(--radius-md);
}

.article-content h3 {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    font-weight: 700;
    margin: 2rem 0 1rem;
    color: #000000;
    background: var(--primary-white);
    padding: 1rem 1.5rem;
    border-left: 4px solid var(--primary-red);
    border-radius: var(--radius-md);
}

.article-content ul,
.article-content ol {
    margin: 1.5rem 0;
    padding: 1rem 1.5rem 1rem 3rem;
    list-style: initial;
    background: var(--primary-white);
    border-radius: var(--radius-md);
}

.article-content li {
    margin-bottom: 0.75rem;
    line-height: 1.8;
    font-size: 1.05rem;
    color: #000000;
}

.article-content blockquote {
    border-left: 5px solid var(--primary-red);
    padding: 1.5rem 2rem;
    margin: 2rem 0;
    background: var(--primary-white);
    border-radius: var(--radius-md);
    font-style: italic;
    font-size: 1.15rem;
    color: #000000;
}

.article-content blockquote cite {
    display: block;
    margin-top: 1rem;
    font-style: normal;
    font-size: 0.9rem;
    color: var(--gray-700);
    font-weight: 600;
}

.info-box {
    background: var(--primary-white);
    padding: 1.5rem;
    border-radius: var(--radius-lg);
    border-left: 5px solid var(--primary-red);
    margin: 2rem 0;
}

.info-box h3 {
    margin-top: 0;
    margin-bottom: 1rem;
    font-size: 1.25rem;
    color: #000000;
}

.info-box ol {
    margin: 0;
    padding-left: 1.5rem;
}

.info-box li {
    color: #000000;
}

.article-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    align-items: center;
    padding: 2rem 0;
    border-top: 1px solid var(--gray-200);
    border-bottom: 1px solid var(--gray-200);
    margin: 2rem 0;
}

.article-tags strong {
    margin-right: 0.5rem;
}

.article-tags a {
    padding: 0.5rem 1rem;
    background: var(--gray-100);
    border-radius: var(--radius-md);
    font-size: 0.875rem;
    color: var(--gray-700);
}

.article-tags a:hover {
    background: var(--accent-blue);
    color: var(--primary-white);
}

.article-share {
    margin: 3rem 0;
}

.article-share h3 {
    font-size: 1.25rem;
    margin-bottom: 1rem;
}

.share-buttons {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
}

.share-btn {
    padding: 0.75rem 1.5rem;
    border-radius: var(--radius-md);
    font-weight: 600;
    font-size: 0.9rem;
    transition: var(--transition-base);
}

.share-btn.twitter {
    background: #1DA1F2;
    color: white;
}

.share-btn.facebook {
    background: #4267B2;
    color: white;
}

.share-btn.linkedin {
    background: #0077B5;
    color: white;
}

.share-btn.copy {
    background: var(--gray-200);
    color: var(--gray-800);
}

.share-btn:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
}

.author-bio {
    display: flex;
    gap: 1.5rem;
    padding: 2rem;
    background: var(--gray-50);
    border-radius: var(--radius-lg);
    margin: 3rem 0;
}

.author-bio img {
    width: 100px;
    height: 100px;
    border-radius: 50%;
}

.bio-content h3 {
    font-size: 1.25rem;
    margin-bottom: 0.5rem;
}

.bio-content p {
    margin-bottom: 1rem;
    font-size: 0.95rem;
    color: var(--gray-600);
}

.related-posts {
    margin: 4rem 0;
}

.related-posts h2 {
    font-family: var(--font-heading);
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 2rem;
}

.related-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
}

/* ========================================
   FOOTER
======================================== */

.footer {
    background: var(--primary-black);
    color: var(--gray-300);
    padding: 3rem 0 1.5rem;
}

.footer-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: 3rem;
    margin-bottom: 2rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid var(--gray-700);
}

.footer-col h3 {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--primary-white);
    margin-bottom: 1rem;
}

.footer-col h3 .highlight {
    color: var(--primary-red);
}

.footer-col h4 {
    font-family: var(--font-heading);
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--primary-white);
    margin-bottom: 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--primary-red);
    display: inline-block;
}

.footer-col p {
    font-size: 0.9rem;
    line-height: 1.6;
    margin-bottom: 1.5rem;
}

.social-links {
    display: flex;
    gap: 1rem;
}

.social-links a {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: var(--gray-800);
    color: var(--primary-white);
    transition: var(--transition-base);
}

.social-links a:hover {
    background: var(--primary-red);
    transform: translateY(-3px);
}

.footer-col ul {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
}

.footer-col ul a {
    font-size: 0.9rem;
    color: var(--gray-400);
    transition: var(--transition-base);
}

.footer-col ul a:hover {
    color: var(--primary-red);
    padding-left: 0.5rem;
}

.footer-bottom {
    text-align: center;
    padding-top: 1.5rem;
    font-size: 0.875rem;
    color: var(--gray-500);
}

/* ========================================
   RESPONSIVE DESIGN
======================================== */

@media (max-width: 1024px) {
    .content-grid {
        grid-template-columns: 1fr;
    }
    
    .posts-grid {
        grid-template-columns: 1fr;
    }
    
    .footer-grid {
        grid-template-columns: repeat(2, 1fr);
    }
    
    .related-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 768px) {
    .nav-menu {
        position: fixed;
        top: 70px;
        left: -100%;
        width: 100%;
        height: calc(100vh - 70px);
        background: var(--primary-white);
        flex-direction: column;
        padding: 2rem;
        transition: left var(--transition-base);
        box-shadow: var(--shadow-lg);
    }
    
    .nav-menu.active {
        left: 0;
    }
    
    .mobile-toggle {
        display: flex;
    }
    
    .hero {
        padding: 4rem 0;
    }
    
    .hero-title {
        font-size: 2.5rem;
    }
    
    .hero-buttons {
        flex-direction: column;
    }
    
    .hero-buttons .btn {
        width: 100%;
    }
    
    .post-card.featured {
        grid-template-columns: 1fr;
    }
    
    .post-card.featured .post-title {
        font-size: 1.5rem;
    }
    
    .article-title {
        font-size: 2rem;
    }
    
    .footer-grid {
        grid-template-columns: 1fr;
        gap: 2rem;
    }
    
    .author-bio {
        flex-direction: column;
        text-align: center;
    }
    
    .author-bio img {
        margin: 0 auto;
    }
}

@media (max-width: 480px) {
    .hero-title {
        font-size: 2rem;
    }
    
    .section-header {
        flex-direction: column;
        align-items: flex-start;
    }
    
    .filter-tags {
        width: 100%;
        overflow-x: auto;
    }
}

